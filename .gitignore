# Dependencies
node_modules/

# Local dev artifacts
temporary screenshots/
*.log

# Environment / secrets
.env
.env.local
.env.*.local

# Internal pricing source documents — this repo is public.
# The customer-facing pricing lives in the Pricing section of the site instead.
Pricing/

# Client platform source, copied here only as a reference for building the
# embedded live demo. This repo is public and that code carries real client
# identifiers — it must never be committed. Ignore the whole tree.
locksmith_website/

# Belt-and-braces. The rule above already covers these, but a plaintext
# credential dump is too dangerous to protect with a single rule that a future
# edit might loosen. Never commit these anywhere in the tree.
pw/
**/pw/
**/.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

.vercel
.env*
